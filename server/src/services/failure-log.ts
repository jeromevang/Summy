/**
 * Failure Log Service
 * Provides a centralized mechanism for logging and managing operational failures.
 */

import { failureLog } from './failure-log/FailureLogService.js';

/**
 * The main failure log instance.
 */
export { failureLog };

/**
 * Default export of the failure log instance.
 */
export default failureLog;